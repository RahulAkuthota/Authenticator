{
  "name": "authenticator-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "dashboard",
    "sdk",
    "react-sdk",
    "demo-app"
  ],
  "scripts": {
    "start": "npm run start --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
